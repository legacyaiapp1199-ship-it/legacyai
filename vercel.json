{
  "buildCommand": "pnpm install && pnpm run build",
  "outputDirectory": ".",
  "devCommand": "pnpm run dev",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
